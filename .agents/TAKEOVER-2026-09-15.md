# Takeover review - 2026-09-15

Scope: understand and summarize the existing relay; no runtime changes,
device access, InfluxDB upload, or deployment.

## Local and remote state

- Reviewed local `main` at `c300aca`. Its cached `origin/main` also pointed
  there, but the GitHub API reported actual remote `main` at `eedb35c`.
- Remote is two commits ahead of the reviewed baseline: `c4404bb` summarizes
  successful-upload logs; `eedb35c` retains optional pressure as `None` in
  the record and relies on InfluxDB serialization to omit it on the wire.
- Reviewed the complete remote comparison. These commits also update tests
  and agent notes; they do not resolve the measurement mismatch.
- No fetch, pull, merge, reset, or remote runtime validation was performed.
  Recheck remote HEAD before continuing; earlier handoff notes describe local
  behavior, not all changes already present on GitHub.
- Existing unstaged changes are deletion of
  `manuals/saes-sip_power-user_manual-rev_4.pdf` and an untracked copy under
  `device-docs/`. Both have Git blob ID
  `b660ff277a5ce57215096517dc4383d0cc35826b`: an exact content-preserving move.
  These user changes were left untouched. README and PROTOCOL still reference
  the old path.

## Current verification

On the local application at `c300aca`, with the existing manual move:

- `uv run pytest -q`: 16 passed, 2 failed. Both failures are measurement
  expectations (`SAESSIPPower` versus actual `seas-sip-power`).
- `uv run ruff check .`: passed.
- `git diff --check 139d9a3 HEAD`: existing trailing whitespace in the
  settings-template host example.
- Local settings use a 2-second interval, host `192.168.50.34`, default UDP
  port 2527, and a 3-second timeout. The template interval is 30 seconds;
  comments differ from local settings.
- No credential contents were inspected. Tests used synthetic source and
  writer doubles. Test output saying "Uploaded" is not a real upload.
- Historical live evidence remains the read-only check dated 2026-08-28.
  Actual deployment, uploaded history, and Grafana dependencies were not
  established by this review.

## Skill and reference refresh

The explicitly supplied `to-influxdb-development` skill was read from
`Sinclair-Agent-Skills/skills/to-influxdb-development`. Its source preflight
reported `status=current-dirty`, ahead 0 / behind 0 on
`feature/to-influxdb-development`; existing skill edits were preserved.
Corpus, modernization, and author-preference references were read.

The current organization inventory contains 13 accessible relay candidates:
`dataq`, `hicube-neo`, `iqair`, `koheron_ctl`, `LFI3751`, `multivisor`, `nut`,
`pico-tc08`, `seas-pump`, `sensorpush`, `ULE-Ion-pump`, `arroyo`, and
`siglent-spd3000` (each with the `-to-influxdb` suffix). All default to `main`
except LFI3751 (`master`); none is archived, and Pico is marked as a fork.
The last two are additions relative to the loaded 11-repository corpus.
Infrastructure, device libraries, and credential repositories were excluded.

This was an inventory refresh for a takeover, not a complete new implementation
corpus audit. Current `main.py` files were additionally compared in ULE (`main`),
LFI3751 (`master`), and NUT (`main`), confirming the historical provenance of
direct polling and immediate retry. No family-wide design change was inferred.

## Next work

Reconcile the remote commits with this checkout while preserving the manual
move. Then resolve the intended measurement name and any actual downstream
dependencies; align tests and README accordingly. Reconcile manual paths and
settings comments. Run offline checks before a new read-only live check.
The present review does not authorize an upload or deployment.
